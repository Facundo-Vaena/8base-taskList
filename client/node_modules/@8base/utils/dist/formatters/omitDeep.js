"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.omitDeep = void 0;

var R = _interopRequireWildcard(require("ramda"));

var omitDeep = function omitDeep(omitedProps, objectForOmitting) {
  if (!objectForOmitting || !R.is(Object, objectForOmitting)) {
    return objectForOmitting;
  }

  var currentLevelOmitedObject = R.omit(omitedProps, objectForOmitting);

  var omitValue = function omitValue(value) {
    if (R.is(Array, value)) {
      return value.map(function (item) {
        return omitDeep(omitedProps, item);
      });
    } else if (R.is(Object, value)) {
      return omitDeep(omitedProps, value);
    }

    return value;
  };

  var fullOmitedObject = R.mapObjIndexed(omitValue, currentLevelOmitedObject);
  return fullOmitedObject;
};

exports.omitDeep = omitDeep;